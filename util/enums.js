const STATUS_ENUMS = [`pending`, `accepted`, `rejected`];
const ROLES_ENUMS = {
    Admin: 'admin',
    Seller: 'seller',
    User: 'user',
};
module.exports = {
    STATUS_ENUMS,
    ROLES_ENUMS,
};
